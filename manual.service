[Unit]
Description=Manual Docusaurus Site
After=network.target

[Service]
Type=simple
User=exedev
WorkingDirectory=/home/exedev/manual
ExecStart=/usr/bin/npm run serve -- --host 0.0.0.0 --port 8000
Restart=on-failure
RestartSec=10
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
